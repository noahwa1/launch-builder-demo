<% content_for :page_title, @campaign.title %>
<% content_for :page_actions do %>
  <%= link_to 'Edit', edit_manage_campaign_path(@campaign), class: 'btn btn-primary btn-sm' %>
  <%= button_to 'Duplicate', duplicate_manage_campaign_path(@campaign), method: :post, class: 'btn btn-secondary btn-sm', form: { class: 'd-inline' }, data: { confirm: 'Duplicate this campaign for a new project?' } %>
  <%= link_to 'All Campaigns', manage_campaigns_path, class: 'btn btn-secondary btn-sm' %>
<% end %>

<style>
  .checklist-toggle { display: inline-flex; align-items: center; gap: 8px; padding: 8px 12px; border-radius: 8px; border: 1px solid var(--border); cursor: pointer; font-size: 13px; transition: all 0.15s; text-decoration: none; color: var(--text-primary); }
  .checklist-toggle:hover { border-color: var(--accent); background: var(--accent-wash); }
  .checklist-toggle.complete { background: rgba(34,197,94,0.06); border-color: rgba(34,197,94,0.2); }
  .checklist-toggle .dot { width: 8px; height: 8px; border-radius: 50%; }
  .checklist-toggle .dot.not_started { background: var(--text-muted); }
  .checklist-toggle .dot.in_progress { background: var(--accent); }
  .checklist-toggle .dot.complete { background: #22C55E; }
  .asset-review-card { border: 1px solid var(--border); border-radius: var(--radius-md); padding: 16px; margin-bottom: 12px; }
  .asset-review-actions { display: flex; gap: 8px; margin-top: 12px; }
  .notes-input { width: 100%; margin-top: 8px; }
</style>

<!-- Stats -->
<div class="stats-grid">
  <div class="stat-card">
    <div class="label">Author</div>
    <div class="text-lg fw-600 mt-8"><%= @campaign.author.full_name %></div>
  </div>
  <div class="stat-card">
    <div class="label">Progress</div>
    <div class="value"><%= @campaign.progress_percentage %>%</div>
  </div>
  <div class="stat-card">
    <div class="label">Status</div>
    <div class="mt-8"><span class="status-badge status-<%= @campaign.status %>"><%= @campaign.status.titleize %></span></div>
  </div>
  <div class="stat-card">
    <div class="label">Pending Review</div>
    <div class="value"><%= @pending_assets.count %></div>
  </div>
</div>

<%# ── Campaign Phase ── %>
<div class="card mb-20">
  <div class="section-header" style="margin-bottom:14px;">
    <h3>Campaign Phase</h3>
    <div class="d-flex gap-8 items-center">
      <% if @campaign.can_advance? %>
        <%= button_to "Advance to #{Campaign.phases.key(Campaign.phases[@campaign.phase] + 1)&.titleize}", advance_phase_manage_campaign_path(@campaign), method: :patch, class: 'btn btn-primary btn-sm', form: { class: 'd-inline' } %>
      <% end %>
    </div>
  </div>

  <%# Phase progress bar %>
  <div class="d-flex gap-4 mb-16">
    <% Campaign.phases.each do |name, idx| %>
      <% current_idx = Campaign.phases[@campaign.phase] %>
      <% is_current = name == @campaign.phase %>
      <% is_past = idx < current_idx %>
      <% is_future = idx > current_idx %>
      <div class="flex-1 text-center">
        <div style="height:6px;border-radius:3px;margin-bottom:6px;background:<%= is_past ? '#16A34A' : (is_current ? 'var(--accent)' : 'var(--border)') %>;"></div>
        <div class="text-xs text-upper" style="font-weight:<%= is_current ? '700' : '500' %>;color:<%= is_current ? 'var(--accent)' : (is_past ? '#16A34A' : 'var(--text-muted)') %>;letter-spacing:0.3px;"><%= name.titleize %></div>
      </div>
    <% end %>
  </div>

  <%# Blockers %>
  <% blockers = @campaign.phase_blockers %>
  <% if blockers.any? %>
    <div class="rounded-md mb-12" style="padding:12px 16px;background:rgba(245,158,11,0.04);border:1px solid rgba(245,158,11,0.15);">
      <div class="text-sm fw-600 text-warning" style="margin-bottom:6px;">Blockers for advancing past <%= @campaign.phase.titleize %></div>
      <ul style="margin:0;padding-left:18px;">
        <% blockers.each do |b| %>
          <li class="text-md text-secondary" style="margin-bottom:2px;"><%= b %></li>
        <% end %>
      </ul>
    </div>
  <% elsif !%w[live wrap_up].include?(@campaign.phase) %>
    <div class="rounded-md mb-12" style="padding:10px 16px;background:rgba(34,197,94,0.04);border:1px solid rgba(34,197,94,0.15);">
      <div class="text-md fw-600 text-success">All clear — ready to advance</div>
    </div>
  <% end %>

  <%# Manual phase override %>
  <details class="mt-4">
    <summary class="text-sm text-muted" style="cursor:pointer;">Manual phase override</summary>
    <div class="d-flex gap-6 flex-wrap mt-8">
      <% Campaign.phases.each_key do |name| %>
        <% if name != @campaign.phase %>
          <%= button_to name.titleize, set_phase_manage_campaign_path(@campaign, phase: name), method: :patch, class: 'btn btn-secondary btn-sm', form: { class: 'd-inline' }, data: { confirm: "Override phase to #{name.titleize}?" } %>
        <% else %>
          <span class="text-sm fw-600 text-accent rounded" style="padding:6px 14px;background:var(--accent-wash);"><%= name.titleize %> (current)</span>
        <% end %>
      <% end %>
    </div>
  </details>
</div>

<%# ── Campaign Brief & Dates ── %>
<% if @campaign.brief.present? || @campaign.launch_date.present? || @campaign.content_deadline.present? %>
<div class="card mb-20">
  <div class="section-header">
    <h3>Campaign Brief</h3>
    <%= link_to 'Edit', edit_manage_campaign_path(@campaign), class: 'btn btn-secondary btn-sm' %>
  </div>
  <% if @campaign.brief.present? %>
    <div class="text-base text-secondary mb-16 p-16" style="line-height:1.7;white-space:pre-wrap;background:var(--page-bg);border-radius:var(--radius-sm);"><%= @campaign.brief %></div>
  <% end %>
  <div class="d-grid gap-12" style="grid-template-columns:repeat(auto-fit,minmax(160px,1fr));">
    <% if @campaign.launch_date.present? %>
      <div style="padding:12px;background:var(--page-bg);border-radius:var(--radius-sm);border-left:3px solid var(--accent);">
        <div class="label-upper">Launch Date</div>
        <div class="fw-700 mt-4" style="font-size:15px;"><%= @campaign.launch_date.strftime('%b %d, %Y') %></div>
        <% days_until = (@campaign.launch_date - Date.today).to_i %>
        <div class="text-xs" style="color:<%= days_until > 14 ? '#16A34A' : (days_until > 0 ? '#D97706' : 'var(--text-muted)') %>;margin-top:2px;">
          <%= days_until > 0 ? "#{days_until} days away" : (days_until == 0 ? 'Today!' : "#{days_until.abs} days ago") %>
        </div>
      </div>
    <% end %>
    <% if @campaign.content_deadline.present? %>
      <div style="padding:12px;background:var(--page-bg);border-radius:var(--radius-sm);border-left:3px solid #F59E0B;">
        <div class="label-upper">Content Deadline</div>
        <div class="fw-700 mt-4" style="font-size:15px;"><%= @campaign.content_deadline.strftime('%b %d, %Y') %></div>
        <% days_until_cd = (@campaign.content_deadline - Date.today).to_i %>
        <div class="text-xs" style="color:<%= days_until_cd > 7 ? '#16A34A' : (days_until_cd > 0 ? '#D97706' : '#EF4444') %>;margin-top:2px;">
          <%= days_until_cd > 0 ? "#{days_until_cd} days left" : (days_until_cd == 0 ? 'Due today' : "#{days_until_cd.abs} days overdue") %>
        </div>
      </div>
    <% end %>
    <% if @campaign.review_deadline.present? %>
      <div style="padding:12px;background:var(--page-bg);border-radius:var(--radius-sm);border-left:3px solid #8B5CF6;">
        <div class="label-upper">Review Deadline</div>
        <div class="fw-700 mt-4" style="font-size:15px;"><%= @campaign.review_deadline.strftime('%b %d, %Y') %></div>
        <% days_until_rd = (@campaign.review_deadline - Date.today).to_i %>
        <div class="text-xs" style="color:<%= days_until_rd > 7 ? '#16A34A' : (days_until_rd > 0 ? '#D97706' : '#EF4444') %>;margin-top:2px;">
          <%= days_until_rd > 0 ? "#{days_until_rd} days left" : (days_until_rd == 0 ? 'Due today' : "#{days_until_rd.abs} days overdue") %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<% end %>

<%# ── Pre-filled Details (Links, Ad Access, Logistics) ── %>
<% has_details = [@campaign.signed_editions_url, @campaign.ad_access_notes, @campaign.bookplate_address, @campaign.management_emails].any?(&:present?) %>
<% if has_details %>
<div class="card mb-20">
  <div class="section-header">
    <h3>Pre-filled Details</h3>
    <%= link_to 'Edit', edit_manage_campaign_path(@campaign), class: 'btn btn-secondary btn-sm' %>
  </div>
  <div class="grid-2">
    <% if @campaign.signed_editions_url.present? %>
      <div>
        <div class="label-upper mb-4">Signed Editions URL</div>
        <a href="<%= @campaign.signed_editions_url %>" target="_blank" class="text-md text-accent" style="word-break:break-all;"><%= @campaign.signed_editions_url %></a>
      </div>
    <% end %>
    <% if @campaign.management_emails.present? %>
      <div>
        <div class="label-upper mb-4">Management Emails</div>
        <div class="text-md"><%= @campaign.management_emails %></div>
      </div>
    <% end %>
    <% if @campaign.bookplate_address.present? %>
      <div>
        <div class="label-upper mb-4">Bookplate Address</div>
        <div class="text-md" style="white-space:pre-wrap;"><%= @campaign.bookplate_address %></div>
      </div>
    <% end %>
    <% if @campaign.bookplate_design.present? %>
      <div>
        <div class="label-upper mb-4">Bookplate Design</div>
        <div class="text-md">Uploaded</div>
      </div>
    <% end %>
  </div>
  <% if @campaign.ad_access_notes.present? %>
    <div class="mt-16" style="padding-top:16px;border-top:1px solid var(--border);">
      <div class="label-upper" style="margin-bottom:6px;">Ad Access Instructions</div>
      <div class="text-md text-secondary p-12" style="white-space:pre-wrap;background:var(--page-bg);border-radius:var(--radius-sm);"><%= @campaign.ad_access_notes %></div>
    </div>
  <% end %>
</div>
<% end %>

<%# ── Creator Confirmations Status ── %>
<div class="card mb-20">
  <div class="section-header">
    <h3>Creator Confirmations</h3>
    <% conf_count = @campaign.confirmable_sections.count { |s| @confirmations[s]&.persisted? && !@confirmations[s]&.has_changes_requested? } %>
    <span class="text-md fw-600 text-muted"><%= conf_count %>/<%= @campaign.confirmable_sections.size %> confirmed</span>
  </div>
  <div class="d-grid gap-10" style="grid-template-columns:repeat(auto-fill, minmax(200px, 1fr));">
    <% @campaign.confirmable_sections.each do |section| %>
      <% conf = @confirmations[section] %>
      <div class="rounded-lg" style="padding:14px;border:1px solid <%= conf&.persisted? ? (conf.has_changes_requested? ? 'rgba(245,158,11,0.3)' : 'rgba(34,197,94,0.3)') : 'var(--border)' %>;background:<%= conf&.persisted? ? (conf.has_changes_requested? ? 'rgba(245,158,11,0.02)' : 'rgba(34,197,94,0.02)') : 'var(--surface)' %>;">
        <div class="d-flex items-center gap-8" style="margin-bottom:6px;">
          <% if conf&.persisted? && !conf.has_changes_requested? %>
            <svg viewBox="0 0 24 24" fill="none" stroke="#16A34A" stroke-width="2" width="16" height="16"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          <% elsif conf&.has_changes_requested? %>
            <svg viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2" width="16" height="16"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
          <% else %>
            <span class="d-inline-block rounded-full border-strong" style="width:16px;height:16px;"></span>
          <% end %>
          <span class="text-base fw-600"><%= section.titleize %></span>
        </div>
        <% if conf&.persisted? && !conf.has_changes_requested? %>
          <div class="text-sm fw-500 text-success">Confirmed <%= conf.confirmed_at.strftime('%b %d') %></div>
        <% elsif conf&.has_changes_requested? %>
          <div class="text-sm fw-600 text-warning">Changes Requested</div>
          <div class="text-sm text-secondary mt-4" style="background:rgba(245,158,11,0.04);padding:6px 8px;border-radius:6px;"><%= conf.notes %></div>
        <% else %>
          <div class="text-sm text-muted">Awaiting creator review</div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<%# ── Campaign Type & Features ── %>
<div class="card mb-20">
  <div class="section-header">
    <h3>Campaign Type & Features</h3>
    <%= link_to 'Edit', edit_manage_campaign_path(@campaign), class: 'btn btn-secondary btn-sm' %>
  </div>
  <div class="gap-row mb-16">
    <% type_config = Campaign::CAMPAIGN_TYPES[@campaign.campaign_type] || Campaign::CAMPAIGN_TYPES['full'] %>
    <span class="text-md fw-700 text-accent rounded-md" style="padding:5px 14px;background:var(--accent-wash);"><%= type_config[:label] %></span>
    <span class="text-md text-secondary"><%= type_config[:description] %></span>
  </div>
  <div class="d-grid gap-8" style="grid-template-columns:repeat(auto-fill, minmax(170px, 1fr));">
    <% Campaign::FEATURE_FLAGS.each do |flag| %>
      <% enabled = @campaign.send("#{flag}_enabled?") %>
      <div class="d-flex items-center gap-8 rounded-md border" style="padding:8px 12px;background:<%= enabled ? 'rgba(34,197,94,0.04)' : 'var(--page-bg)' %>;">
        <span class="rounded-full" style="width:8px;height:8px;background:<%= enabled ? '#16A34A' : '#D1D5DB' %>;flex-shrink:0;"></span>
        <span class="text-md fw-500" style="color:<%= enabled ? 'var(--text-primary)' : 'var(--text-muted)' %>;"><%= flag.titleize %></span>
      </div>
    <% end %>
  </div>
</div>

<%# ── Onboarding Example Category ── %>
<div class="card mb-20">
  <h3>Onboarding Examples</h3>
  <p class="text-secondary text-md mb-12">Choose which example campaigns the creator sees during their welcome onboarding.</p>
  <%= form_with model: @campaign, url: update_settings_manage_campaign_path(@campaign), method: :patch, class: 'gap-row' do |f| %>
    <%= f.select :example_category, options_for_select(Campaign::EXAMPLE_CATEGORIES.map { |c| [c.titleize, c] }, @campaign.example_category), { include_blank: 'None (default)' }, style: 'padding:8px 12px;border:1px solid var(--border);border-radius:8px;font-size:13px;background:var(--surface);min-width:180px;' %>
    <button type="submit" class="btn btn-primary btn-sm">Save</button>
    <% if @campaign.onboarding_completed? %>
      <span class="text-muted text-sm">Onboarding completed <%= time_ago_in_words(@campaign.onboarding_completed_at) %> ago</span>
    <% else %>
      <span class="text-sm text-warning">Not yet onboarded</span>
    <% end %>
  <% end %>
</div>

<%# ── Personal Videos Toggle ── %>
<div class="card mb-20">
  <h3>Personal Videos</h3>
  <p class="text-secondary text-md mb-12">When enabled, creators can record rapid-fire personalized videos for each buyer from the recorder Queue mode.</p>
  <div class="gap-row">
    <%= form_with model: @campaign, url: update_settings_manage_campaign_path(@campaign), method: :patch, class: 'd-inline' do |f| %>
      <%= f.hidden_field :personal_videos_enabled, value: !@campaign.personal_videos_enabled? %>
      <button type="submit" class="btn <%= @campaign.personal_videos_enabled? ? 'btn-success' : 'btn-secondary' %> btn-sm">
        Personal Videos: <%= @campaign.personal_videos_enabled? ? 'ON' : 'OFF' %>
      </button>
    <% end %>
    <% if @campaign.personal_videos_enabled? %>
      <% pv_total = @campaign.landing_page&.page_submissions&.count || 0 %>
      <% pv_done = @campaign.personal_videos.count %>
      <span class="text-muted text-sm"><%= pv_done %>/<%= pv_total %> videos recorded</span>
    <% end %>
  </div>
</div>

<div class="two-col">
  <!-- Checklist -->
  <div class="card">
    <h3>Checklist</h3>
    <% Campaign::CATEGORIES.each do |cat| %>
      <% items = @items_by_category[cat] || [] %>
      <% next if items.empty? %>
      <div class="mb-16">
        <div class="text-xs fw-700 text-upper text-muted mb-8" style="letter-spacing:0.5px;"><%= cat.titleize %></div>
        <% items.each do |item| %>
          <%= button_to toggle_checklist_item_manage_campaign_path(@campaign, checklist_item_id: item.id), method: :patch, class: "checklist-toggle #{item.status}", form: { class: 'd-inline' } do %>
            <span class="dot <%= item.status %>"></span>
            <%= item.title %>
            <% if item.optional? %><span class="text-muted" style="font-size:10px;">(optional)</span><% end %>
          <% end %>
          <br>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Deliverables -->
  <% if @campaign.deliverables_enabled? %>
    <div class="card mb-16">
      <div class="section-header">
        <h3>Deliverables</h3>
        <%= link_to 'Manage', manage_campaign_admin_deliverables_path(@campaign), class: 'btn btn-secondary btn-sm' %>
      </div>
      <% revisions = @campaign.admin_deliverables.needs_revision %>
      <% pending_del = @campaign.admin_deliverables.pending %>
      <% if revisions.any? %>
        <div style="padding:10px 14px;background:rgba(239,68,68,0.04);border-left:3px solid #EF4444;border-radius:0 8px 8px 0;margin-bottom:10px;">
          <div class="text-sm fw-600" style="color:#EF4444;"><%= revisions.count %> revision<%= 's' if revisions.count != 1 %> requested</div>
          <% revisions.limit(3).each do |del| %>
            <div class="text-md mt-4">
              <%= link_to del.title, manage_campaign_admin_deliverable_path(@campaign, del), class: 'text-primary fw-500' %>
            </div>
          <% end %>
        </div>
      <% end %>
      <div class="d-flex gap-16 text-md">
        <span><strong><%= @campaign.admin_deliverables.count %></strong> total</span>
        <span class="text-warning"><strong><%= pending_del.count %></strong> awaiting review</span>
        <span class="text-success"><strong><%= @campaign.admin_deliverables.approved.count %></strong> approved</span>
      </div>
      <div class="mt-12">
        <%= link_to 'New Deliverable', new_manage_campaign_admin_deliverable_path(@campaign), class: 'btn btn-primary btn-sm' %>
      </div>
    </div>
  <% end %>

  <!-- Assets Pending Review -->
  <div>
    <div class="card">
      <h3>Assets Pending Review (<%= @pending_assets.count %>)</h3>
      <% if @pending_assets.any? %>
        <% @pending_assets.each do |asset| %>
          <div class="asset-review-card">
            <div class="d-flex justify-between items-center">
              <div>
                <div class="text-base fw-600"><%= asset.original_filename || 'Unnamed' %></div>
                <div class="text-muted text-sm"><%= asset.asset_type.titleize %> — uploaded <%= time_ago_in_words(asset.created_at) %> ago</div>
              </div>
              <span class="status-badge status-pending">Pending</span>
            </div>
            <div class="asset-review-actions">
              <%= button_to 'Approve', approve_manage_campaign_campaign_asset_path(@campaign, asset), method: :patch, class: 'btn btn-success btn-sm', form: { class: 'd-inline' } %>
              <%= form_with url: request_changes_manage_campaign_campaign_asset_path(@campaign, asset), method: :patch, class: 'd-flex gap-6 flex-1' do %>
                <input type="text" name="admin_notes" placeholder="Notes for creator..." class="notes-input flex-1" style="padding:6px 10px;border:1px solid var(--border);border-radius:6px;font-size:13px;">
                <button type="submit" class="btn btn-danger btn-sm">Request Changes</button>
              <% end %>
            </div>
          </div>
        <% end %>
      <% else %>
        <div class="centered-empty text-muted">No assets pending review.</div>
      <% end %>
    </div>

    <!-- Landing Page -->
    <% if @landing_page %>
      <div class="card">
        <h3>Landing Page</h3>
        <% if @landing_page.build_requested? %>
          <div class="rounded-md mb-16" style="padding:12px 16px;background:rgba(234,179,8,0.08);border:1px solid rgba(234,179,8,0.2);">
            <div class="text-md fw-600" style="color:#CA8A04;">Build Requested</div>
            <div class="text-muted text-sm" style="margin-top:2px;">
              <%= @landing_page.build_requested_at ? "#{time_ago_in_words(@landing_page.build_requested_at)} ago" : '' %> by <%= @campaign.author.full_name %>
            </div>
          </div>
        <% end %>
        <div class="d-flex items-center gap-8 mb-12">
          <span class="status-badge status-<%= @landing_page.published? ? 'live' : 'draft' %>"><%= @landing_page.published? ? 'Published' : 'Draft' %></span>
          <% if @landing_page.has_content? %>
            <span class="text-muted text-sm">Has content</span>
          <% else %>
            <span class="text-muted text-sm">No content yet</span>
          <% end %>
        </div>
        <div class="d-flex gap-8 flex-wrap items-center">
          <%= link_to builder_manage_campaign_landing_page_path(@campaign), class: 'btn btn-primary btn-sm' do %>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18"/><path d="M9 21V9"/></svg>
            Open Builder
          <% end %>
          <div class="d-inline-block" style="position:relative;" id="generateDropdown">
            <button type="button" class="btn btn-secondary btn-sm" onclick="document.getElementById('generateMenu').style.display = document.getElementById('generateMenu').style.display === 'block' ? 'none' : 'block'">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
              Generate Page ▾
            </button>
            <div id="generateMenu" style="display:none;position:absolute;top:100%;left:0;margin-top:4px;background:#fff;border:1px solid var(--border-strong);border-radius:8px;box-shadow:var(--shadow-lg);z-index:20;min-width:220px;padding:4px;">
              <% LandingPageGenerator::TEMPLATES.each do |key, label| %>
                <%= button_to generate_manage_campaign_landing_page_path(@campaign, template: key), method: :patch, form: { class: 'd-block' }, style: 'display:block;width:100%;text-align:left;padding:8px 12px;font-size:13px;border:none;background:none;cursor:pointer;border-radius:6px;font-family:inherit;color:var(--text-primary);', data: { turbo_confirm: @landing_page.has_content? ? "This will overwrite the current page content with the \"#{label}\" template. Continue?" : nil }, onmouseover: "this.style.background='var(--accent-wash)'", onmouseout: "this.style.background='none'" do %>
                  <%= label %>
                <% end %>
              <% end %>
            </div>
          </div>
          <script>document.addEventListener('click',function(e){if(!document.getElementById('generateDropdown').contains(e.target))document.getElementById('generateMenu').style.display='none';});</script>
          <%= link_to wizard_manage_campaign_landing_page_path(@campaign), class: 'btn btn-secondary btn-sm items-center gap-4', style: 'display:inline-flex;' do %>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 013 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
            Page Wizard
          <% end %>
          <% if @landing_page.page_submissions.any? %>
            <%= link_to manage_campaign_page_submissions_path(@campaign), class: 'btn btn-secondary btn-sm' do %>
              Submissions (<%= @landing_page.page_submissions.count %>)
            <% end %>
          <% end %>
        </div>

        <%# Notification toggle %>
        <div class="d-flex items-center gap-8 mt-12">
          <%= button_to toggle_notifications_manage_campaign_landing_page_path(@campaign), method: :patch, class: 'btn btn-secondary btn-sm', form: { class: 'd-inline' } do %>
            <%= @landing_page.notify_on_submission? ? 'Notifications: ON' : 'Notifications: OFF' %>
          <% end %>
          <span class="text-muted text-sm">Email creator on new form submission</span>
        </div>
      </div>
    <% end %>

    <%# Live Events card %>
    <% live_events = @campaign.live_events.recent.limit(5) %>
    <% if live_events.any? || @campaign.is_live? %>
    <div class="card">
      <h3>Live Events</h3>
      <% if @campaign.is_live? %>
        <div class="rounded-md mb-12 p-12" style="background:rgba(233,69,96,0.08);border:1px solid rgba(233,69,96,0.2);">
          <span class="text-md fw-700" style="color:#e94560;">LIVE NOW — <%= @campaign.current_live_event&.title %></span>
        </div>
      <% end %>
      <% live_events.each do |event| %>
        <div class="d-flex items-center justify-between border-bottom" style="padding:8px 0;">
          <span class="text-base"><%= event.title %></span>
          <span class="status-badge status-<%= event.live? ? 'live' : event.ended? ? 'draft' : 'pending' %>"><%= event.status.titleize %></span>
        </div>
      <% end %>
      <%= link_to 'All Events', manage_campaign_live_events_path(@campaign), class: 'btn btn-secondary btn-sm mt-12' %>
    </div>
    <% end %>

    <% approved = @all_assets.where(status: :approved) %>
    <% if approved.any? %>
      <div class="card">
        <h3>Approved Assets (<%= approved.count %>)</h3>
        <% approved.each do |asset| %>
          <div class="d-flex items-center justify-between border-bottom" style="padding:8px 0;">
            <span class="text-base"><%= asset.original_filename || asset.asset_type.titleize %></span>
            <span class="status-badge status-approved">Approved</span>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<%# ── Activity Feed ── %>
<div class="card mb-20">
  <div class="section-header" style="margin-bottom:14px;">
    <h3>Activity Feed</h3>
    <span class="text-sm text-muted"><%= @activities.size %> events</span>
  </div>

  <% if @activities.any? %>
    <div style="max-height:500px;overflow-y:auto;">
      <% @activities.group_by { |a| a.created_at.to_date }.each do |date, day_activities| %>
        <div class="text-xs fw-600 text-muted text-upper border-bottom mb-8" style="letter-spacing:0.5px;padding:8px 0 4px;">
          <%= date == Date.today ? 'Today' : date == Date.yesterday ? 'Yesterday' : date.strftime('%B %d, %Y') %>
        </div>
        <% day_activities.each do |activity| %>
          <div class="d-flex gap-10 items-start" style="padding:8px 0;border-bottom:1px solid rgba(0,0,0,0.04);">
            <div class="d-flex items-center justify-center text-xs fw-700 rounded-full" style="width:28px;height:28px;background:<%= activity.user&.admin? ? 'var(--accent-wash)' : 'rgba(34,197,94,0.08)' %>;color:<%= activity.user&.admin? ? 'var(--accent)' : '#16A34A' %>;flex-shrink:0;">
              <%= activity.user_initials %>
            </div>
            <div class="flex-1" style="min-width:0;">
              <div class="text-md text-primary"><%= activity.human_description %></div>
              <div class="text-xs text-muted" style="margin-top:2px;">
                <%= activity.user&.first_name || 'System' %> &middot; <%= activity.created_at.strftime('%-I:%M %p') %>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <div class="centered-empty text-muted text-md">No activity recorded yet.</div>
  <% end %>
</div>

<%# ── Page Wizard Modal ── %>
<% if @landing_page %>
<div id="wizardOverlay" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:1000;overflow-y:auto;padding:40px 16px;">
<div class="mx-auto rounded-xl" style="max-width:720px;background:#fff;box-shadow:0 20px 60px rgba(0,0,0,0.3);">
  <!-- Header -->
  <div class="d-flex items-center justify-between border-bottom" style="padding:20px 24px;">
    <div>
      <h2 class="text-xl fw-700" style="margin:0;">Page Wizard</h2>
      <p class="text-muted text-sm" style="margin:2px 0 0;">Step <span id="wizardStepNum">1</span> of 3</p>
    </div>
    <button onclick="closeWizard()" style="background:none;border:none;font-size:24px;cursor:pointer;color:var(--text-muted);line-height:1;">&times;</button>
  </div>

  <!-- Step 1: Choose Template -->
  <div id="wizardStep1" class="p-24">
    <h3 class="text-lg mb-16">Choose a Template</h3>
    <div class="d-grid gap-12" style="grid-template-columns:repeat(auto-fill,minmax(200px,1fr));">
      <% icons = { 'standard' => '&#128214;', 'personalized_video' => '&#127909;', 'preorder_incentive' => '&#127873;', 'address_capture' => '&#128230;', 'landing_page' => '&#127760;', 'vinyl' => '&#127926;' } %>
      <% descs = { 'standard' => 'Book page with author bio', 'personalized_video' => 'Steps + receipt form + video', 'preorder_incentive' => 'Bonus cards + receipt form', 'address_capture' => 'Shipping address form', 'landing_page' => 'Showcase + email signup', 'vinyl' => 'Dark theme for music' } %>
      <% LandingPageGenerator::TEMPLATES.each do |key, label| %>
        <div class="wizard-tpl-card text-center p-16 rounded-lg" data-tpl="<%= key %>" onclick="wizardSelectTemplate('<%= key %>')" style="border:2px solid var(--border);cursor:pointer;transition:all 0.15s;">
          <div class="mb-8" style="font-size:28px;"><%= icons[key]&.html_safe %></div>
          <div class="text-base fw-600"><%= label %></div>
          <div class="text-xs text-muted mt-4"><%= descs[key] %></div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Step 2: Customize Content -->
  <div id="wizardStep2" class="p-24" style="display:none;max-height:60vh;overflow-y:auto;">
    <h3 class="text-lg mb-16">Customize Content</h3>

    <%# Hero — all templates %>
    <fieldset class="border rounded-md p-16 mb-16" id="wiz-section-hero">
      <legend class="text-md fw-700" style="padding:0 6px;">Hero</legend>
      <div class="mb-12"><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Headline</label><input type="text" id="wiz-hero-headline" value="<%= h(@campaign.submission.title.presence || 'Your Book Title') %>" class="input-md"></div>
      <div class="mb-12"><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Subheadline</label><input type="text" id="wiz-hero-subheadline" value="by <%= h(@campaign.author.full_name) %>" class="input-md"></div>
      <div class="grid-2" style="gap:12px;">
        <div><label class="d-block text-sm fw-600" style="margin-bottom:3px;">CTA Text</label><input type="text" id="wiz-hero-cta-text" value="Order Your Copy" class="input-md"></div>
        <div><label class="d-block text-sm fw-600" style="margin-bottom:3px;">CTA URL</label><input type="text" id="wiz-hero-cta-url" value="<%= h(@campaign.signed_editions_url.presence || '#') %>" class="input-md"></div>
      </div>
    </fieldset>

    <%# Book — all templates %>
    <fieldset class="border rounded-md p-16 mb-16" id="wiz-section-book">
      <legend class="text-md fw-700" style="padding:0 6px;">Book</legend>
      <div class="mb-12"><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Title</label><input type="text" id="wiz-book-title" value="<%= h(@campaign.submission.title.presence || '') %>" class="input-md"></div>
      <div class="mb-12"><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Description</label><textarea id="wiz-book-description" rows="3" class="input-md" style="resize:vertical;"><%= h(@campaign.submission.description.presence || '') %></textarea></div>
      <div class="grid-2" style="gap:12px;">
        <div><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Genre</label><input type="text" id="wiz-book-genre" value="<%= h(@campaign.submission.genre.presence || 'General') %>" class="input-md"></div>
        <div><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Release Date</label><input type="text" id="wiz-book-release-date" value="<%= @campaign.submission.release_date&.strftime('%B %d, %Y') || 'Coming Soon' %>" class="input-md"></div>
      </div>
    </fieldset>

    <%# Author — standard, landing_page, address_capture, vinyl %>
    <fieldset class="border rounded-md p-16 mb-16" id="wiz-section-author">
      <legend class="text-md fw-700" style="padding:0 6px;">Author</legend>
      <div class="mb-12"><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Name</label><input type="text" id="wiz-author-name" value="<%= h(@campaign.author.full_name) %>" class="input-md"></div>
      <div><label class="d-block text-sm fw-600" style="margin-bottom:3px;">Bio</label><textarea id="wiz-author-bio" rows="3" class="input-md" style="resize:vertical;"><%= h(@campaign.author.description.presence || '') %></textarea></div>
    </fieldset>

    <%# Steps — personalized_video, preorder_incentive %>
    <fieldset class="border rounded-md p-16 mb-16" style="display:none;" id="wiz-section-steps">
      <legend class="text-md fw-700" style="padding:0 6px;">How It Works (Steps)</legend>
      <div id="wiz-steps-list">
        <% 4.times do |i| %>
        <div class="d-grid gap-8 items-center mb-8" style="grid-template-columns:auto 1fr 2fr;">
          <span class="text-sm fw-700 text-muted"><%= i+1 %>.</span>
          <input type="text" class="wiz-step-title input-sm" data-idx="<%= i %>" value="" placeholder="Step title">
          <input type="text" class="wiz-step-desc input-sm" data-idx="<%= i %>" value="" placeholder="Description">
        </div>
        <% end %>
      </div>
    </fieldset>

    <%# Retailers — personalized_video, preorder_incentive, landing_page %>
    <fieldset class="border rounded-md p-16 mb-16" style="display:none;" id="wiz-section-retailers">
      <legend class="text-md fw-700" style="padding:0 6px;">Retailers</legend>
      <div class="grid-2" style="gap:6px;">
        <% [['Amazon','amazon'], ['Barnes & Noble','barnes_noble'], ['Bookshop.org','bookshop'], ['Target','target'], ['Walmart','walmart'], ['Books-A-Million','bam']].each do |name, key| %>
          <label class="d-flex items-center gap-6 text-md" style="cursor:pointer;"><input type="checkbox" class="wiz-retailer" data-name="<%= name %>" checked> <%= name %></label>
        <% end %>
      </div>
    </fieldset>

    <%# Testimonials — personalized_video, landing_page, standard, vinyl %>
    <fieldset class="border rounded-md p-16 mb-16" id="wiz-section-testimonials">
      <legend class="text-md fw-700" style="padding:0 6px;">Testimonials</legend>
      <div id="wiz-testimonials-list">
        <div class="wiz-testimonial-row d-grid gap-8 mb-8" style="grid-template-columns:2fr 1fr;">
          <input type="text" class="wiz-testimonial-quote input-sm" placeholder="Quote text..." value="&quot;An absolutely captivating read from start to finish.&quot;">
          <input type="text" class="wiz-testimonial-attr input-sm" placeholder="- Attribution" value="- Reader Review">
        </div>
      </div>
      <button type="button" onclick="wizardAddTestimonial()" class="text-sm text-accent" style="background:none;border:none;cursor:pointer;padding:4px 0;">+ Add testimonial</button>
    </fieldset>

    <%# Bonuses — preorder_incentive only %>
    <fieldset class="border rounded-md p-16 mb-16" style="display:none;" id="wiz-section-bonuses">
      <legend class="text-md fw-700" style="padding:0 6px;">Bonus Items</legend>
      <div id="wiz-bonuses-list">
        <% [['&#9997;&#65039;', 'Signed Bookplate', 'A personal signed bookplate from the author'], ['&#128214;', 'Bonus Chapter', 'An exclusive bonus chapter not in the final book'], ['&#127873;', 'Limited Print', 'Exclusive art print (first 500 orders)']].each_with_index do |(emoji, title, desc), i| %>
        <div class="wiz-bonus-row d-grid gap-8 items-center mb-8" style="grid-template-columns:50px 1fr 2fr;">
          <input type="text" class="wiz-bonus-emoji text-center" data-idx="<%= i %>" value="<%= emoji %>" style="padding:6px;border:1px solid var(--border);border-radius:6px;font-size:16px;">
          <input type="text" class="wiz-bonus-title input-sm" data-idx="<%= i %>" value="<%= title %>">
          <input type="text" class="wiz-bonus-desc input-sm" data-idx="<%= i %>" value="<%= desc %>">
        </div>
        <% end %>
      </div>
    </fieldset>
  </div>

  <!-- Step 3: Review & Generate -->
  <div id="wizardStep3" class="p-24" style="display:none;">
    <h3 class="text-lg mb-16">Review & Generate</h3>
    <div class="rounded-md mb-20" style="background:var(--surface-alt,#f8f9fa);padding:20px;">
      <div class="mb-12"><span class="text-sm fw-600 text-muted">Template</span><div id="wiz-review-template" class="fw-600" style="font-size:15px;margin-top:2px;">Standard</div></div>
      <div class="mb-12"><span class="text-sm fw-600 text-muted">Sections Included</span><div id="wiz-review-sections" class="text-md text-primary" style="margin-top:2px;">—</div></div>
      <div><span class="text-sm fw-600 text-muted">Customized Fields</span><div id="wiz-review-fields" class="text-md text-primary" style="margin-top:2px;">—</div></div>
    </div>
    <% if @landing_page.has_content? %>
      <p class="text-sm mb-12" style="color:#b45309;">This will overwrite the current page content.</p>
    <% end %>
  </div>

  <!-- Footer -->
  <div class="d-flex justify-between" style="padding:16px 24px;border-top:1px solid var(--border);">
    <button type="button" id="wizardBackBtn" onclick="wizardBack()" style="display:none;padding:8px 16px;border:1px solid var(--border);border-radius:6px;background:#fff;font-size:13px;cursor:pointer;">Back</button>
    <div class="ml-auto d-flex gap-8">
      <button type="button" onclick="closeWizard()" style="padding:8px 16px;border:1px solid var(--border);border-radius:6px;background:#fff;font-size:13px;cursor:pointer;">Cancel</button>
      <button type="button" id="wizardNextBtn" onclick="wizardNext()" class="btn btn-primary btn-sm" style="padding:8px 20px;">Next</button>
    </div>
  </div>
</div>
</div>

<style>
  .wizard-tpl-card:hover { border-color: var(--accent) !important; background: var(--accent-wash); }
  .wizard-tpl-card.selected { border-color: var(--accent) !important; background: var(--accent-wash); box-shadow: 0 0 0 1px var(--accent); }
</style>

<script>
(function(){
  var wizardStep = 1;
  var wizardTemplate = 'standard';

  var tplSections = {
    standard:           { hero:true, book:true, author:true, steps:false, retailers:false, testimonials:true, bonuses:false },
    personalized_video: { hero:true, book:true, author:true, steps:true, retailers:true, testimonials:true, bonuses:false },
    preorder_incentive: { hero:true, book:true, author:false, steps:true, retailers:true, testimonials:false, bonuses:true },
    address_capture:    { hero:true, book:true, author:true, steps:false, retailers:false, testimonials:false, bonuses:false },
    landing_page:       { hero:true, book:true, author:true, steps:false, retailers:true, testimonials:true, bonuses:false },
    vinyl:              { hero:true, book:true, author:true, steps:false, retailers:false, testimonials:true, bonuses:false }
  };

  var tplStepDefaults = {
    personalized_video: [
      { title: 'Order the Book', desc: 'Purchase from any retailer below' },
      { title: 'Submit Your Receipt', desc: 'Upload your proof of purchase' },
      { title: 'Get Confirmed', desc: "We'll verify your order" },
      { title: 'Enjoy Your Video', desc: 'Receive a personalized video message' }
    ],
    preorder_incentive: [
      { title: 'Pre-Order the Book', desc: 'Purchase from any retailer below' },
      { title: 'Submit Your Receipt', desc: 'Upload your proof of purchase' },
      { title: 'Get Confirmed', desc: "We'll verify your pre-order" },
      { title: 'Enjoy Your Bonus', desc: 'Receive your exclusive bonus content' }
    ]
  };

  var tplCTADefaults = {
    standard: 'Order Your Copy',
    personalized_video: 'Get Your Personalized Video',
    preorder_incentive: 'Pre-Order Now',
    address_capture: 'Claim Your Signed Copy',
    landing_page: 'Learn More',
    vinyl: 'Order Vinyl'
  };

  var templateNames = <%= raw LandingPageGenerator::TEMPLATES.to_json %>;

  window.openWizard = function() {
    document.getElementById('wizardOverlay').style.display = 'block';
    showStep(1);
    wizardSelectTemplate('standard');
  };

  window.closeWizard = function() {
    document.getElementById('wizardOverlay').style.display = 'none';
  };

  window.wizardSelectTemplate = function(key) {
    wizardTemplate = key;
    document.querySelectorAll('.wizard-tpl-card').forEach(function(el){
      el.classList.toggle('selected', el.dataset.tpl === key);
    });
    document.getElementById('wiz-hero-cta-text').placeholder = tplCTADefaults[key] || 'Order Your Copy';
    if (!document.getElementById('wiz-hero-cta-text')._userEdited) {
      document.getElementById('wiz-hero-cta-text').value = tplCTADefaults[key] || 'Order Your Copy';
    }
    var stepDefaults = tplStepDefaults[key] || tplStepDefaults['personalized_video'];
    document.querySelectorAll('.wiz-step-title').forEach(function(el, i) {
      if (stepDefaults[i] && !el._userEdited) { el.value = stepDefaults[i].title; }
    });
    document.querySelectorAll('.wiz-step-desc').forEach(function(el, i) {
      if (stepDefaults[i] && !el._userEdited) { el.value = stepDefaults[i].desc; }
    });
    wizardUpdateSections();
  };

  function wizardUpdateSections() {
    var vis = tplSections[wizardTemplate] || tplSections.standard;
    ['hero','book','author','steps','retailers','testimonials','bonuses'].forEach(function(s){
      var el = document.getElementById('wiz-section-' + s);
      if (el) el.style.display = vis[s] ? '' : 'none';
    });
  }

  document.addEventListener('input', function(e) {
    if (e.target.id && e.target.id.startsWith('wiz-')) e.target._userEdited = true;
    if (e.target.classList.contains('wiz-step-title') || e.target.classList.contains('wiz-step-desc')) e.target._userEdited = true;
  });

  window.wizardAddTestimonial = function() {
    var list = document.getElementById('wiz-testimonials-list');
    if (list.querySelectorAll('.wiz-testimonial-row').length >= 3) return;
    var row = document.createElement('div');
    row.className = 'wiz-testimonial-row';
    row.style.cssText = 'display:grid;grid-template-columns:2fr 1fr;gap:8px;margin-bottom:8px;';
    row.innerHTML = '<input type="text" class="wiz-testimonial-quote input-sm" placeholder="Quote text...">' +
                    '<input type="text" class="wiz-testimonial-attr input-sm" placeholder="- Attribution">';
    list.appendChild(row);
  };

  function showStep(n) {
    wizardStep = n;
    document.getElementById('wizardStep1').style.display = n === 1 ? '' : 'none';
    document.getElementById('wizardStep2').style.display = n === 2 ? '' : 'none';
    document.getElementById('wizardStep3').style.display = n === 3 ? '' : 'none';
    document.getElementById('wizardStepNum').textContent = n;
    document.getElementById('wizardBackBtn').style.display = n > 1 ? '' : 'none';
    document.getElementById('wizardNextBtn').textContent = n === 3 ? 'Generate Page' : 'Next';
    if (n === 2) wizardUpdateSections();
    if (n === 3) buildReview();
  }

  function buildReview() {
    document.getElementById('wiz-review-template').textContent = templateNames[wizardTemplate] || wizardTemplate;
    var vis = tplSections[wizardTemplate] || tplSections.standard;
    var secs = [];
    if (vis.hero) secs.push('Hero');
    if (vis.book) secs.push('Book');
    if (vis.author) secs.push('Author');
    if (vis.steps) secs.push('How It Works');
    if (vis.retailers) secs.push('Retailers');
    if (vis.testimonials) secs.push('Testimonials');
    if (vis.bonuses) secs.push('Bonuses');
    secs.push('Footer');
    document.getElementById('wiz-review-sections').textContent = secs.join(', ');
    var total = 0, customized = 0;
    var fieldIds = ['wiz-hero-headline','wiz-hero-subheadline','wiz-hero-cta-text','wiz-hero-cta-url','wiz-book-title','wiz-book-description','wiz-book-genre','wiz-book-release-date','wiz-author-name','wiz-author-bio'];
    fieldIds.forEach(function(id) {
      var el = document.getElementById(id);
      if (el) { total++; if (el.value.trim()) customized++; }
    });
    document.getElementById('wiz-review-fields').textContent = customized + ' of ' + total + ' fields populated';
  }

  window.wizardNext = function() {
    if (wizardStep === 3) { wizardGenerate(); return; }
    showStep(wizardStep + 1);
  };

  window.wizardBack = function() {
    if (wizardStep > 1) showStep(wizardStep - 1);
  };

  function collectWizardData() {
    var vis = tplSections[wizardTemplate] || tplSections.standard;
    var data = { template: wizardTemplate };
    data.hero = {
      headline: document.getElementById('wiz-hero-headline').value,
      subheadline: document.getElementById('wiz-hero-subheadline').value,
      cta_text: document.getElementById('wiz-hero-cta-text').value,
      cta_url: document.getElementById('wiz-hero-cta-url').value
    };
    data.book = {
      description: document.getElementById('wiz-book-description').value,
      genre: document.getElementById('wiz-book-genre').value,
      release_date: document.getElementById('wiz-book-release-date').value
    };
    if (vis.author) {
      data.author = {
        name: document.getElementById('wiz-author-name').value,
        bio: document.getElementById('wiz-author-bio').value
      };
    }
    if (vis.steps) {
      data.steps = [];
      document.querySelectorAll('.wiz-step-title').forEach(function(el, i) {
        var desc = document.querySelectorAll('.wiz-step-desc')[i];
        if (el.value.trim()) {
          data.steps.push({ number: String(i+1), title: el.value, description: desc ? desc.value : '' });
        }
      });
    }
    if (vis.retailers) {
      var defaultUrls = { 'Amazon': 'https://amazon.com', 'Barnes & Noble': 'https://barnesandnoble.com', 'Bookshop.org': 'https://bookshop.org', 'Target': 'https://target.com', 'Walmart': 'https://walmart.com', 'Books-A-Million': 'https://booksamillion.com' };
      var defaultIcons = { 'Amazon': "\uD83D\uDCE6", 'Barnes & Noble': "\uD83D\uDCDA", 'Bookshop.org': "\uD83C\uDFEA", 'Target': "\uD83C\uDFAF", 'Walmart': "\uD83D\uDED2", 'Books-A-Million': "\uD83D\uDCD6" };
      data.retailers = [];
      document.querySelectorAll('.wiz-retailer:checked').forEach(function(el) {
        var name = el.dataset.name;
        data.retailers.push({ name: name, url: defaultUrls[name] || '#', icon: defaultIcons[name] || "\uD83D\uDCDA" });
      });
    }
    if (vis.testimonials) {
      data.testimonials = [];
      document.querySelectorAll('.wiz-testimonial-row').forEach(function(row) {
        var q = row.querySelector('.wiz-testimonial-quote');
        var a = row.querySelector('.wiz-testimonial-attr');
        if (q && q.value.trim()) {
          data.testimonials.push({ quote: q.value, attribution: a ? a.value : '' });
        }
      });
    }
    if (vis.bonuses) {
      data.bonuses = [];
      document.querySelectorAll('.wiz-bonus-row').forEach(function(row) {
        var emoji = row.querySelector('.wiz-bonus-emoji');
        var title = row.querySelector('.wiz-bonus-title');
        var desc = row.querySelector('.wiz-bonus-desc');
        if (title && title.value.trim()) {
          data.bonuses.push({ emoji: emoji ? emoji.value : '', title: title.value, description: desc ? desc.value : '' });
        }
      });
    }
    return data;
  }

  function wizardGenerate() {
    var data = collectWizardData();
    var form = document.createElement('form');
    form.method = 'POST';
    form.action = '<%= wizard_generate_manage_campaign_landing_page_path(@campaign) %>';
    form.style.display = 'none';
    var csrfParam = document.querySelector('meta[name="csrf-param"]');
    var csrfToken = document.querySelector('meta[name="csrf-token"]');
    if (csrfParam && csrfToken) {
      var inp = document.createElement('input');
      inp.type = 'hidden'; inp.name = csrfParam.content; inp.value = csrfToken.content;
      form.appendChild(inp);
    }
    var methodInput = document.createElement('input');
    methodInput.type = 'hidden'; methodInput.name = '_method'; methodInput.value = 'patch';
    form.appendChild(methodInput);
    function addField(name, value) {
      var inp = document.createElement('input');
      inp.type = 'hidden'; inp.name = name; inp.value = value;
      form.appendChild(inp);
    }
    function flattenToForm(obj, prefix) {
      for (var key in obj) {
        if (!obj.hasOwnProperty(key)) continue;
        var val = obj[key];
        var fieldName = prefix ? prefix + '[' + key + ']' : key;
        if (Array.isArray(val)) {
          val.forEach(function(item, idx) {
            if (typeof item === 'object' && item !== null) {
              flattenToForm(item, fieldName + '[' + idx + ']');
            } else {
              addField(fieldName + '[]', item);
            }
          });
        } else if (typeof val === 'object' && val !== null) {
          flattenToForm(val, fieldName);
        } else {
          addField(fieldName, val);
        }
      }
    }
    flattenToForm(data, 'wizard_data');
    document.body.appendChild(form);
    form.submit();
  }

  document.getElementById('wizardOverlay').addEventListener('click', function(e) {
    if (e.target === this) closeWizard();
  });
})();
</script>
<% end %>
